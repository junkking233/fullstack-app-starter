# API 接口文档 API

本文件由阶段 4 生成或更新。接口未定稿前保持精简，不提前虚构接口。

## 1. 角色与模块

| 角色 | 模块 | 可执行操作 |
| --- | --- | --- |
|  |  |  |

## 2. 接口清单

| 模块 | 方法 | 路径 | 用途 | 权限 | 状态 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  | 待设计 |

## 3. 接口详情模板

### 接口名称

- 方法与路径：
- 用途：
- 允许角色：
- 请求参数：
- 返回字段：
- 错误码：
- 业务说明：
- 前端/小程序调用页面：

## 4. 统一约定

- 响应结构：`code`、`message`、`data`、`timestamp`。
- 成功默认：`code: 200`，`message` 使用中文成功提示，例如 `操作成功`。
- 列表接口必须说明分页、筛选和排序。
- 权限接口必须说明登录态、允许角色和越权返回。
