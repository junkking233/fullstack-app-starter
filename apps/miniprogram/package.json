{
  "name": "business-app-miniprogram",
  "version": "0.1.0",
  "private": true,
  "description": "Native WeChat Mini Program client for business app scaffold.",
  "scripts": {
    "install:dev": "npm install",
    "dev": "npm run install:dev",
    "check": "node scripts/check.js",
    "build": "npm run check"
  },
  "dependencies": {
    "@vant/weapp": "1.11.7"
  }
}
