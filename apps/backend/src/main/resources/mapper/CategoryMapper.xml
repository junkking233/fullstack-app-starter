<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhiqu.community.mapper.CategoryMapper">

    <select id="selectTree" resultType="com.zhiqu.community.entity.Category">
        SELECT * FROM t_category ORDER BY level, sort_order
    </select>

    <select id="countQuestionsUnderCategory" resultType="int">
        SELECT COUNT(*) FROM t_question
        WHERE category_id IN (
            SELECT id FROM t_category WHERE id = #{categoryId} OR parent_id = #{categoryId}
        )
        AND status = 'published'
    </select>

</mapper>
