# 原型生图提示词模板 LovartPromptTemplate

本模板用于阶段 2，指导 Agent 生成 `design/lovart/原型生图提示词-LovartPrompt.md`。目标是生成可被 Lovart 出图、Stitch 重建和 Figma 还原使用的单页开发稿提示词，不是生成作品集展示图。

阶段 2 完成时，必须运行 `python3 scripts/workflow.py sync`，从正式文档自动生成根目录 `index.html` 的 Lovart 提示词翻页数据。该翻页区只在当前阶段为 2 时显示。

阶段 2 必须先探测当前环境实际可用的 Lovart 能力标识、认证、额度和模型，获得用户对页面数量与本批额度消耗的明确批准，再把单页提示词作为输入生成 P0/P1 原型图。未安装能力、未配置认证、未获额度批准、生成失败或没有输出文件时，必须写入 GoalPlan 阻塞项与恢复动作。

## 核心原则

1. 先读取 `docs/产品需求文档-PRD.md` 的 `ScopeBudget`。
2. 只为 P0/P1 主链路写生图提示词。
3. P2、暂缓功能、待确认功能不生成单页开发稿。
4. 不生成作品集总览图、交互概览图、独立状态稿或独立弹层稿。
5. 弹窗、底部弹层、表单错误、空态、加载态、成功提示只写入对应页面的“必要状态备注”。
6. 所有页面共用同一套设计系统、导航规则、组件风格、颜色、字号、圆角和图标风格。
7. Lovart 出图后进入 Stitch 重建 UI screen，再由 Figma MCP 同步到目标 Figma 文件形成可编辑 Figma Frame；后续代码还原只使用具体 Figma 页面 Frame。
8. 每个页面的完整提示词必须能被 `workflow.py sync` 解析，一张图对应一张卡片。
9. Lovart、Stitch 和 Figma 都是必选链路；缺少任一环节都必须阻塞，不能推进下一阶段。
10. 先生成 1 张代表页面并获得用户方向确认，再批量生成其余页面。
11. 正式文档的每个单页提示词必须使用 `### P01-页面名称` 标题和紧随其后的 `text` 代码块，供 `workflow.py sync` 自动解析，禁止手工维护 HTML 提示词数组。

## 输出结构

### 1. 产品与设计目标

- APP 名称：
- 产品定位：
- 目标用户和角色：
- 核心业务场景：
- 本次 Lovart 出图目标：

### 2. 页面清单 ScreenInventory

首轮页面预算：单端建议不超过 8-10 个首版页面，多端合计超过 12 个页面时，先回到 PRD 合并角色、合并页面或删减功能。

| 编号 | 页面 | 类型 | 角色 | 关联 P0/P1 功能 | 入口/触发 | 导航规则 | 主要内容 | 必要状态备注 | Lovart 用途 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P01 |  | 一级 Tab 页面 / 二级功能页面 / 详情页面 / 表单页面 |  |  |  |  |  |  | 单页开发稿 |

暂缓页面：

| 页面/功能 | 暂缓原因 | 后续条件 |
| --- | --- | --- |
|  | P2 / 超出角色功能预算 / 待用户确认 |  |

### 3. 全局设计系统 DesignSystemPrompt

```text
【全局设计系统】
产品名称：{APP名称}
设计风格：{由 PRD 判断，例如现代、干净、可信、校园服务、效率感}
主色：{主色}
辅助色：{辅助色}
背景：浅色背景，页面留白舒适。
字体：中文 UI 字体，层级清晰，文字必须清晰可读。
组件：圆角卡片、明确按钮、线性图标、状态标签、真实表单控件。
布局：{按端类型选择：APP/微信小程序为 9:16 竖屏完整页面，包含状态栏、安全区和真实移动端间距；网页为 16:9 完整页面，包含真实网页导航、内容区和响应式间距。}
统一要求：所有页面颜色、字号、圆角、阴影、图标风格、按钮样式一致。
禁止：错误端类型、PC 后台、低龄卡通、杂乱拼贴、文字乱码、底部裁切、组件漂移、不同页面风格不一致。
```

### 4. 导航规则 NavigationContract

```text
【导航规则】
APP/微信小程序一级 Tab 页面必须显示底部 Tab：{Tab1}、{Tab2}、{Tab3}、{Tab4}。
底部 Tab 固定在屏幕底部，4 个等宽排列，包含图标和文字。
当前页面对应 Tab 必须高亮，其他 Tab 弱化。
底部 Tab 高度约 64px，并保留 iPhone safe area 和 Home Indicator。
页面内容区必须预留底部导航高度，最后一块内容不能被底部 Tab 遮挡。

网页页面使用顶部导航或侧栏导航，不显示移动端底部 Tab；画面按 16:9 完整网页页面生成。

二级功能页面、详情页面、表单页面默认不显示底部 Tab。
二级页面顶部必须有返回按钮；如有底部主操作按钮，必须使用固定底部操作区并保留 safe area。

弹窗、底部弹层、筛选面板、表单错误、空态、加载态和成功提示只作为对应页面的必要状态备注，不单独生成 Lovart 图片。
```

### 5. 单页开发稿提示词 SinglePagePrompts

正式文档中每个页面分别使用以下标题与代码块格式：

### P01-页面名称

```text
【Pxx-页面名称】
生成目标：只生成 1 张 1K 完整单页开发稿，不要生成多屏拼贴，不要生成交互流程图。APP/微信小程序使用 9:16 竖屏，网页使用 16:9。

引用全局设计系统：
【粘贴 DesignSystemPrompt】

页面类型：{一级 Tab 页面 / 二级功能页面 / 详情页面 / 表单页面}
用户角色：{角色}
关联 P0/P1 功能：{来自 PRD ScopeBudget 的功能名称}
页面目标：{这个页面帮助用户完成什么任务}

导航规则：
{说明是否显示底部 Tab；如果显示，写清当前高亮 Tab；如果不显示，写清返回按钮和底部操作区}

页面结构：
1. 顶部区域：{标题、学校/位置、消息、返回等}
2. 核心内容：{模块和信息层级}
3. 操作区域：{主按钮、次按钮、固定底部操作区}
4. 状态信息：{金额、距离、时间、订单状态、权限状态等}
5. 必要状态备注：{只用文字说明弹窗、底部弹层、错误态、空态、成功提示，不要求 Lovart 单独出图}

真实数据示例：
{写入 3-6 条真实中文样例，避免 Lovart 用乱码和空白占位}

验收要求：
端类型正确，APP/小程序页面顶部状态栏和底部安全区完整，网页页面导航和内容区完整；文字清晰，页面内容不遮挡导航或操作按钮，风格与其他页面一致。

禁止项：
不要错误端类型、不要后台管理页、不要多设备并排、不要作品集拼贴、不要交互概览图、不要独立状态图、不要低龄卡通、不要文字乱码、不要底部导航裁切。
```

### 6. 出图与归档

- 单页开发稿 PNG：`design/lovart/pages/P01-首页.png`
- 单页开发稿 PSD：`design/lovart/pages/P01-首页.psd`
- `index.html` 阶段 2 Lovart 翻页复制区：运行 `python3 scripts/workflow.py sync`，由正式 Prompt 文档自动生成。
- Lovart 必选记录：实际能力标识、认证/额度、用户批准、代表页确认、输出文件、Project/thread、模型、1K、画幅、fast 使用结果、原模式/恢复结果、失败原因写入 GoalPlan。
- Stitch 重建记录：Project、screenId、来源 Lovart 图、页面提示词、htmlCode/designSystem/截图备份、目标 Figma 文件、Figma MCP 同步方式、Figma Frame 链接/nodeId 和可编辑节点检查结果写入 `docs/Goal任务计划-GoalPlan.md`。
- Figma Frame：记录到 `docs/设计还原文档-UIDesign.md` 和 `docs/Goal任务计划-GoalPlan.md`

### 7. 必选 Lovart 能力执行规则

- 必须调用当前环境实际可用的 Lovart 能力自动出图；先记录能力标识、认证、额度和用户批次批准，缺任一项时阶段 2 必须阻塞。
- 先生成 1 张代表页面并获得用户方向确认，再开始其余页面的受控批次。
- 每个新业务需求都新建 Lovart Project，Project 名称使用业务中文名称，不复用旧业务项目。
- 生成前先调用 `query-mode` 记录原账户模式，获得用户额度批准后临时调用 `set-mode --fast`；每个新页面 thread 显式使用 `--mode fast`。批次正常完成或失败退出后恢复原模式，用户明确要求保持 fast 时除外。
- 每次生成后重新检查并校正 Project 名称，防止本地状态被 prompt 前缀覆盖。
- Lovart 多页面出图必须受控并发执行：同一业务共用同一个 Lovart Project，每个 P0/P1 页面使用独立新 thread 并显式传入新 Project 的 `--project-id`；默认并发上限为 3，页面数量超过 3 时分批执行。
- 自动出图固定指定图片模型 `generate_image_seedream_v5_pro`，输出 1K；命令显式传入 `--prefer-models '{"IMAGE":["generate_image_seedream_v5_pro"]}'`，实际返回模型不符时该页面不能完成。
- 画幅按端类型决定：APP/微信小程序页面使用 9:16，网页页面使用 16:9；每张正式提示词必须写明“单张 1K + 对应画幅”。
- 自动出图只使用本文件的单页完整提示词，不额外生成作品集总览、交互概览、独立弹窗或独立状态图。
- 每个页面单独生成并保存到 `design/lovart/pages/`，生成命令显式使用 `--prefix "Pxx-页面名称"`；每个页面只生成 1 张单页开发稿，不生成多变体。
- 同一页面微调重试时才允许复用该页面 thread；并发出图遇到 409/429、额度、风控、下载失败或无输出文件时，先记录失败页面和原因，再降低并发或排队重试，不连续无控制重试。
- 如果自动生成失败、额度不足、环境变量缺失或工具不可用，把原因写入 GoalPlan 阻塞项，不得标记阶段 2 完成。
- Lovart 输出图必须进入 Stitch 或 Figma 整理；第 4 步代码实现不能直接以 Lovart PNG/PSD 替代 Figma Frame。

### 8. 必选 Stitch 重建与 Figma 同步规则

- 必须探测到实际可执行的 Stitch 重建/上传能力且 Stitch MCP/API Key 可用；不可用时阶段 3 必须阻塞。
- 必须探测到可写入目标 Figma 文件的 Figma MCP 能力；不可用时阶段 3 必须阻塞。
- 每个新业务需求新建或定位同名 Stitch Project。
- 每个页面只使用当前页面的 Lovart 单页图和对应完整页面提示词，重建 1 个 UI screen。
- 生成 screen 前先确认 Project、图片路径、文件大小和页面编号；不要在未确认时上传文件或消耗额度。
- 不生成多变体、作品集拼贴、交互流程图、独立弹层图或独立状态图。
- Stitch 输出的 screenId、htmlCode、designSystem 和截图写入 `docs/Goal任务计划-GoalPlan.md`，并归档到 `design/stitch/` 或 `.stitch/`。
- Figma MCP 必须把 Stitch screen 同步为目标 Figma 文件中的 `Pxx-页面名` 可编辑 Frame，记录 fileKey、Page、Frame 链接/nodeId、同步方式和节点检查结果。
- 截图只能做视觉对照，不能作为 Figma 中唯一图层；如果 Figma 里只有一张截图图片，没有可编辑 Text、Rectangle、Frame、Component 等设计节点，第 4 步不能开始。
- 手动 Copy/Paste 只允许作为用户明确同意的降级方案；默认不得要求用户手动粘贴。
- 用户还必须明确确认 P0/P1 页面设计可进入开发；Frame 存在不能替代设计确认。
